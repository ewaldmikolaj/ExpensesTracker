@model IEnumerable<ExpensesTracker.Models.Expense>

@{
    ViewData["Title"] = "Index";
}

<div class="d-flex flex-column">
    <h1>Twoje rachunki</h1>
    <div class="mt-2 mb-4">
        <a asp-action="Create" class="text-decoration-none text-white">
            <button class="btn btn-dark">
                Stwórz nowy rachunek
            </button>
        </a>
    </div>
    @foreach (var item in Model)
    {
        <a asp-action="Details" asp-route-id="@item.Id" class="d-block w-100 py-3 px-2 bg-light border rounded text-decoration-none text-black mb-2">   
            <div class="d-flex align-items-center justify-content-between">
                <p class="m-0">@Html.DisplayFor(modelItem => item.Title)</p>
                <i class="bi bi-trash3-fill"></i>
            </div>
        </a>
    }
</div>
